#pragma once

namespace btree {
    template <typename K, typename V>
    class IOManager;

    template <typename K, typename V>
    class Entry;

    template <typename K, typename V>
    struct BTreeNode;
}