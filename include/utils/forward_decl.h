#pragma once

namespace btree {
    template <typename K, typename V>
    class IOManager;

    template <typename K, typename V>
    struct BTree;

    template <typename K, typename V>
    struct BTreeNode;
}